apiVersion: v1
kind: Service
metadata:
  name: browser-chrome
  namespace: sliples
  labels:
    app.kubernetes.io/name: sliples
    app.kubernetes.io/component: browser-chrome
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sliples
    app.kubernetes.io/component: browser-chrome
  ports:
    - name: ws
      port: 3000
      targetPort: ws
      protocol: TCP
