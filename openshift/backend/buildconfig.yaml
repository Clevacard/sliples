apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: sliples-backend
  labels:
    app: sliples
    component: backend
spec:
  source:
    type: Git
    git:
      uri: https://github.com/Clevacard/sliples.git
      ref: main
    contextDir: backend
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: sliples-backend:latest
  triggers:
    - type: ConfigChange
